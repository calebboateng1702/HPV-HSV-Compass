# Uploaded media is served as static files only — never executed as code.
<FilesMatch "\.(php|phtml|php3|php4|php5|php7|phar|pl|py|cgi|sh)$">
  Require all denied
</FilesMatch>
php_flag engine off
Options -Indexes
